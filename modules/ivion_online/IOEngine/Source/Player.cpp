#include <IOEngine/Player.hpp>

namespace IO
{
    namespace Engine
    {

        Player::Player(int idx, int teamIdx) : Index(idx), TeamIndex(teamIdx)
        {
        }
    } // namespace Engine
} // namespace IO