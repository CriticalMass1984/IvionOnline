#include <IOEngine/Card.hpp>

namespace IO
{
    namespace Engine
    {
        Card::Card(Player *player) noexcept : Controller(player)
        {
        }

    } // namespace Engine
} // namespace IO